# VelaShell 优化审查计划

审查日期：2026-09-05  
审查范围：`src/`、`plugins/`、`tests/`、构建与发布脚本、`DESIGN.md`、`plan.md`，并对照 velashell-docs 的宿主架构、交互规格和插件进度约定。本文是审查清单，不代表所有条目都是已确认缺陷；标为“先测量”的项目必须先取得基线再改实现。

## 审查基线

- 技术栈为 .NET 11 preview、Avalonia 12.1、自研 VelaDock、自研 VT 终端渲染、SonnetDB，以及进程内/隔离进程两种插件宿主模式。
- `src/` 与 `plugins/` 共 682 个文件。主要复杂度集中在：`MainWindowViewModel.cs` 4,551 行、`FileBrowserViewModel.cs` 2,924 行、`PluginManager.cs` 2,468 行、`VelaTerminalControl.cs` 3,067 行、AI 插件 `ChatPanelView.axaml.cs` 2,678 行。它们是回归半径和性能热点的首要候选。
- `dotnet build VelaShell.slnx --no-restore`：成功，有 1 条警告：`plugins/VelaShell.Plugin.Ai/Ui/ChatPanelView.Editing.cs:46` 的 CA1859，建议把 `DeleteFromAsync` 的返回类型从 `Task` 改为 `Task<bool>`。
- `dotnet test VelaShell.slnx --no-build --no-restore`：总计 2,785，成功 2,766，跳过 19，耗时约 178.1 秒。跳过项需要在 CI 日志中确认确实打印了 `[SKIP]`，不能只看全绿结果。
- 现有代码已经覆盖许多边界：终端输出合批、资源字典原子换主题、文件浏览器先加载后提交、会话状态合并、插件激活超时与崩溃退避、传输行数上限等。后续优化应保留这些行为，并用基准/回归测试证明没有倒退。

## P0：先处理高影响的并发、取消和帧性能

### P0-1 文件浏览器导航要真正取消旧请求

**证据**：`src/VelaShell/ViewModels/FileBrowserViewModel.cs:1226-1291` 的 `NavigateToAsync` 通过 `_navigationVersion` 丢弃过期结果，但只把 `_lifetime.Token` 传给 `ListDirectoryAsync`，没有为每次导航取消前一个请求。用户快速点击目录、刷新或切换标签时，旧的网络请求仍会运行、占用 SFTP 通道和服务器资源；版本检查只能避免写错列表，不能节省工作。

**建议**：增加“当前导航”`CancellationTokenSource`，新导航先取消并释放旧源，再递增版本；刷新和静默刷新分别使用独立令牌，关闭标签时统一链接到生命周期令牌。保留“最新请求提交”检查，避免某些不支持取消的协议返回旧结果。

**验收**：连续导航 100 次时，旧请求收到取消；最后一次路径唯一提交；取消、断网、关闭标签不产生未观察异常。为 SFTP、FTP 和插件文件系统分别补一个假的慢服务测试。

### P0-2 超大目录需要分页或分批呈现

**证据**：`FileBrowserViewModel` 同时维护 `_allFiles`、排序结果和 `BatchObservableCollection`；`RebuildVisibleFiles()` 会对完整目录过滤、排序并 `ReplaceAll`。`FileBrowserView.axaml` 使用 `ListBox`，容器可能虚拟化，但远端目录数据、ViewModel 和排序仍是全量内存操作，没有分页或流式首屏。

**建议**：先在 `ISftpService`/插件文件系统能力上确认是否能流式列举；能流式时使用 `IAsyncEnumerable` 或页游标，先提交首屏，再按批次追加；不能流式时至少提供目录条目上限、分批排序和“继续加载”入口。对名称排序可采用目录/文件两段排序，避免反复全量 LINQ 链。

**验收**：10 万条模拟目录的首屏在目标时间内出现，峰值内存不随完整目录线性翻倍；滚动、Ctrl/Shift 多选、隐藏文件、排序和刷新选择恢复保持正确。需要记录 1 千、1 万、10 万条的首屏延迟、峰值内存和 GC。

### P0-3 建立终端渲染帧预算，再决定脏行优化

**证据**：`VelaTerminalControl.ApplyOutputUpdate()` 最终调用 `InvalidateTerminal()`；`Render()` 仍逐行遍历当前屏幕并重建各行绘制记录。代码已经有输出合批、GlyphRun 缓存和独立光标层，这是继续优化的良好基础，但尚未有生产场景的 P95 帧时长、输入延迟和分配基线。

**建议**：增加 `EventSource`/诊断计数，分别测量解析、脏行合并、布局、绘制、GC 和输入到回显延迟。若基准显示全屏重绘占主要成本，再增加脏行/脏区集合：同一帧合并重复行，只重绘变化行；光标、选区和搜索高亮继续走独立层。禁止为了计数而在热路径分配字符串或 LINQ 集合。

**验收**：持续输出 100k 行、`htop` 原地刷新、滚动回看、窗口缩放四种场景，记录 60/120Hz 下 P95 帧时间、P95 输入延迟、Gen0 分配；现有像素渲染测试和选择/折叠行为全部通过。

### P0-4 关闭路径不能在 UI 线程同步等待

**证据**：

- `src/VelaShell.Terminal/SshTerminalBridge.cs:100-135` 的 `Dispose()` 调用 `_readTask.Wait(2s)` 与 `_writeTask.Wait(1s)`；
- `src/VelaShell.Infrastructure/Persistence/SonnetDbEngine.cs:98-122` 的 `Dispose()` 调用 `_gate.Wait()`；
- `src/VelaShell.Infrastructure/Plugins/Protocols/PluginTerminalShellStream.cs:45-51` 的 `WriteLine()` 使用 `GetAwaiter().GetResult()`。

这些路径目前有超时或设计注释，但仍可能阻塞 UI、放大网络/插件异常造成的停顿，甚至在回调占用同一调度器时形成死锁。

**建议**：增加 `IAsyncDisposable` 关闭主路径，先发取消/完成信号，再异步等待；同步 `Dispose` 只做不可阻塞的标记和资源释放，或明确仅允许后台调用。数据库提供 `DisposeAsync` 并在应用退出流程中使用；插件终端的同步 `WriteLine` 改为有界写队列/后台发送，至少提供超时和诊断日志。

**验收**：在挂起的读、写、RPC、数据库操作下关闭标签和退出应用，UI 线程不等待网络；关闭总耗时有上限；所有任务最终可观察、可回收。补充重复关闭、关闭期间新输入和插件进程被杀三类测试。

### P0-5 合并高频写入和窗口缩放任务

**证据**：`SshTerminalBridge` 使用 `Channel.CreateUnbounded<OutboundItem>`；插件终端 `Resize()` 每次调用都 `Task.Run` 一次。突发粘贴、广播输入或连续拖拽分隔条时，队列和后台任务可能无界增长。

**建议**：写队列设置容量和背压策略，输入合并为一个有序批次；窗口缩放只保留最新 `(columns, rows)`，用单个发送循环丢弃中间尺寸；记录队列深度、丢弃/合并次数和写延迟。不能丢失用户字节的输入队列与可丢弃的 resize 队列应分开。

**验收**：粘贴 1 MB、广播到多标签、连续调整窗口 5 秒时，队列有上限、最终尺寸正确、输入顺序不变，GC 和线程数不随事件数线性增长。

### P0-6 约束数据库全局串行锁的成本

**证据**：`SonnetDbEngine` 用单个 `SemaphoreSlim _gate` 包住所有集合操作和时序写入；`SonnetDbAppDataStore.GetAllAsync()`、`SonnetDbSessionRepository.GetAll*()` 对集合执行全扫描、反序列化和排序。连接配置、插件数据、审计日志和录制数据增长后，任何慢查询都可能阻塞其他读写。

**建议**：先用基准确认 SonnetDB 的线程安全边界，再按集合拆分锁或建立单写者队列；把序列化移到锁外，把写入批处理；为配置查询增加索引/缓存，为审计和录制增加时间范围查询、保留策略与压缩/归档。不要在未验证 SonnetDB 并发语义前直接放开并发写。

**验收**：并发读取配置、写入审计、录制刷盘和插件 KV 时分别测 P95 等待时间、吞吐、内存和数据一致性；应用退出可等待队列排空或在期限内安全丢弃可重建数据。

## P1：架构与功能优化

### P1-1 拆分主窗口和文件浏览器编排边界

`MainWindowViewModel` 同时负责连接、终端标签、文档、SFTP/FTP/插件工作台、广播、通知、快捷键、状态栏和设置联动；`FileBrowserViewModel` 同时负责列目录、文件操作、传输计划、冲突决策、编辑器和插件动作。建议按以下接口拆出可独立测试的服务：

- `SessionRegistry`：终端/文档会话登记、状态合并、迟到事件过滤和关闭；
- `ConnectionCoordinator`：协议选择、认证、重连、阶段状态和取消；
- `RemoteDirectoryController`：导航、取消、排序、分页和选择恢复；
- `TransferCoordinator`：计划、并发、冲突策略、重试、断点和汇总；
- `CommandRegistry` 与 `CommandPaletteViewModel`：统一命令元数据、上下文、快捷键和禁用原因。

验收是“薄 ViewModel + 服务单测”，而不是机械拆文件；每次拆分都要保留现有会话状态合并和 headless UI 测试。

### P1-2 用协议能力描述符收敛分支

SSH、SFTP、FTP/FTPS 和插件协议已经分别实现默认路径、认证后命令、终端、远程文件、隧道和动态字段。建议将这些能力以版本化描述符声明，宿主根据能力生成连接表单、命令可用性和文件操作入口；插件新增能力必须先在 SDK 仓库设计并发布契约，再抬 NuGet 版本，禁止本地造包或猜版本。

### P1-3 统一错误模型和诊断上下文

现有连接错误已区分认证、超时、网络和插件异常，但文件操作、传输、插件 RPC 和数据库错误仍可能直接显示原始 `Exception.Message`。建议统一 `ErrorKind`、用户文案、可执行动作（重试/编辑/复制诊断/查看日志）和 correlation id；诊断导出默认脱敏主机名、路径、用户名和插件机密。

### P1-4 加强传输队列能力

在已有取消、重试、冲突策略和断点续传上增加：暂停/恢复、失败项批量重试、批量冲突决策、动态带宽限速、流式哈希和可恢复的队列快照。大文件不得先读入完整 `byte[]`；传输面板保留失败项和进行中项，已完成项可按组折叠。

### P1-5 插件运行时增加配额和可观测性

隔离进程已有握手、心跳、激活超时和崩溃退避；下一步增加 IPC 单调用超时/大小上限、并发限制、CPU/内存观测、插件日志采样和连续失败熔断。权限授予记录应可审计、按插件撤销；任何签名/商店策略先对照插件蓝图和 SDK 版本计划，不能在宿主单方面改变安装契约。

### P1-6 构建与测试工程化

- 修复 CA1859，并把 Release 构建提升为零警告门禁；增加阻塞等待、未传递 `CancellationToken`、无界 Channel、可编辑数值 `TextBox` 和颜色/字号字面量扫描。
- 为终端解析/渲染、目录排序/分页、传输计划、SonnetDB 查询、插件 RPC 建立 BenchmarkDotNet 基准和 P95/分配阈值。
- CI 按单元、headless UI、基础设施、Docker 集成和发布测试分层并行；NuGet/构建缓存按锁文件失效；摘要明确哪些测试因环境跳过。
- 对 trimming、ReadyToRun、AOT 只做可重复的体积/启动/反射兼容性实验，结果达标后再纳入发布。

## P1：交互与 UI 优化

### P1-UI-1 连接流程阶段化并可取消

连接对话框已有 `IsBusy` 和错误复制，但连接按钮只表达“忙/不忙”。增加解析、连接、认证、建立通道、加载目录等阶段文案和取消入口；连接失败卡直接提供重试、编辑配置和复制脱敏诊断。连接窗口关闭后发起连接的语义要保持不变。

### P1-UI-2 文件浏览器补齐空态和窄窗口体验

当前视图有加载层和错误横幅，但空目录没有明确的下一步引导。增加“空目录/无权限/未连接”区分、上传/新建文件夹/刷新快捷入口；面包屑过长时折叠中间层并保留完整 Tooltip；保存列宽、排序和滚动位置；窄窗口自动折叠低优先级列和本地面板。危险删除增加短暂撤销窗口，无法撤销时至少显示明确影响范围。

### P1-UI-3 会话树和标签页提供可过滤、可访问状态

增加仅显示活动/错误/收藏的过滤器、批量关闭和拖拽目标预览；状态圆点同时提供本地化可访问名称和文本状态，不能只依赖颜色。标签溢出下拉、键盘上下选择、关闭其他/右侧等操作与命令注册表保持一致。

### P1-UI-4 传输、通知和诊断面板分组

传输面板已有 100 行保护、取消和单项重试，但长批次仍以平面列表呈现。按活动/完成/失败分组，显示批次汇总和失败数量；失败项一键重试，长错误使用详情展开与复制按钮，避免撑坏浮层。连接诊断步骤显示当前阶段、耗时和下一步动作。

### P1-UI-5 命令面板上下文感知

按当前协议、焦点区域和连接状态过滤命令，展示快捷键、禁用原因和最近使用；大量结果采用虚拟化。命令、菜单、标题栏按钮和标签右键菜单都从同一注册表取标题、图标、权限和可用性。

### P1-UI-6 响应式布局、键盘和无障碍

窄窗口自动折叠侧栏/文件区，所有浮层支持 Esc 关闭、焦点回收和稳定 Tab 顺序；为会话树、标签、图标按钮、状态点、错误提示补齐本地化 `AutomationProperties`。当前业务 XAML 中显式无障碍名称很少（主要集中在滚动条、远程/本地刷新按钮），应补充 headless 焦点和屏幕阅读器回归。增加 Reduced Motion 设置，避免动画在低性能或敏感用户环境持续运行。

### P1-UI-7 主题和字体切换保持低成本

继续使用主题资源字典一次性替换；字体/字号未改变时不得清空 glyph cache 或触发全量 relayout。为主题切换、字体切换、背景图加载增加耗时和内存指标，防止新增控件绕过现有约束。

## P2：产品能力候选

这些项目在 `plan.md` 中仍标为缺口，应先做需求和协议可行性确认，再排版本：

1. SSH config 导入：解析 `Host/HostName/Port/User/IdentityFile/ProxyJump`，复用现有多来源导入框架，并提供冲突预览。
2. Anti-idle：与 SSH keepalive 区分，在用户明确开启后按间隔发送可配置的安全字节；终端回显、录制和审计必须明确标记。
3. 用户自定义关键字高亮：运行时规则、颜色 token、正则超时/数量上限和导入导出。
4. 触发器/自动应答：匹配范围、敏感输入保护、逐条审批、循环检测和审计记录。
5. 串口插件与 SSH 证书认证：分别按插件 SDK 能力和 Tmds.Ssh 支持度评估，不在宿主里临时加协议分支。
6. sixel/图形终端能力：先验证渲染后端、缓冲区占用和录制格式，再决定是否进入核心终端。

## 推荐实施顺序

1. 先加入 P0-1、P0-3、P0-4 的基准和回归测试，确认取消、帧时间和关闭延迟的现状。
2. 修复导航取消、同步关闭等待和高频 resize 合并；这些改动风险可控且能直接减少资源浪费。
3. 处理超大目录与数据库串行锁，形成可量化的内存、吞吐和首屏目标。
4. 把会话生命周期、连接编排、传输队列和协议能力逐步抽出，保持每次提交可回滚。
5. 实施连接、文件浏览器、传输面板、命令面板和无障碍改进，补齐 headless/集成测试。
6. 最后评估插件配额、诊断导出、SSH config 导入及 trimming/AOT。

## 变更纪律与风险

- 本文是本仓库的审查计划，不改变运行行为。真正实施后若改变配置、快捷键、UI 规格、插件契约或发布流程，必须同步更新 velashell-docs 的 `zh/` 与 `en/` 镜像。
- UI 颜色、字号和间距继续遵守 `DESIGN.md`，使用 `DynamicResource`，新增文案补齐五份 resx；快捷键先登记 `ShortcutCatalog`。
- 插件契约改动先在 SDK 仓库完成并发布正式 NuGet 包，再升级 `src/Directory.Packages.props` 与测试包版本；禁止本地包和猜测版本号。
- 终端渲染、生命周期拆分和数据库并发改动面最大，应采用小步提交、性能基准和现有全量测试三重保护。
- 每个落地事项应记录影响范围、指标前后值、负责人、目标版本、回滚方案以及是否需要文档 PR，避免优化完成后失去可验证性。

